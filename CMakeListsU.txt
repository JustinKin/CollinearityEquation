cmake_minimum_required(VERSION 3.0.0)

project(TestEigen VERSION 0.1.0)

SET(CMAKE_CXX_COMPILER "/usr/bin/g++") # 默认c++编译器

SET(CMAKE_BUILD_TYPE "Release")  # Debug模式 选项: Release Debug MinSizeRel RelWithDebInfo

SET(EXECUTABLE_OUTPUT_PATH "${PROJECT_SOURCE_DIR}/bin") # 可执行文件输出目录

#命名变量
#set(INC_DIR /usr/local/include)
#set(LINK_DIR /usr/local/lib)
set(SOURCES
    ${PROJECT_SOURCE_DIR}/TestEigen.cpp
    #${PROJECT_SOURCE_DIR}/note.cpp
)


## 头文件包含目录， 相当于gcc/clang 中的-I(i的大写字母)参数
##INCLUDE_DIRECTORIES("/home/justinkin/OpenCV/myinstall/include/opencv4") 
INCLUDE_DIRECTORIES("/opt/intel/mkl/include") 

#去哪里找库文件 .so .dll .dylib 相当于gcc 中的-L参数
link_directories("/opt/intel/mkl/lib/intel64")
link_directories("/opt/intel/lib/intel64")

link_libraries(
    mkl_intel_lp64 
    mkl_intel_thread
    mkl_core
)
link_libraries(
    iomp5
    pthread
    m
    dl
   
)

add_executable(TestEigen ${SOURCES})

#target_link_libraries(note
#    PRIVATE
#       ${PROJECT_SOURCE_DIR}
#)

#要链接的库文件的名字 相当于gcc中的-l(小写的l)参数
#只写库名就可以，就是libxxx.so的中间xxx的部分
target_link_libraries(TestEigen
    mkl_intel_lp64 
    mkl_intel_thread
    mkl_core
    iomp5
    pthread
    m
    dl
)
